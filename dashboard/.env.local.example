# Copy to .env.local — restart `pnpm dev` after edits
# Dev (Vite proxy): leave VITE_API_URL empty for same-origin /api proxy
# VITE_API_URL=
# LAN / production build: point at Flask host
# VITE_API_URL=http://192.168.1.100:5000
VITE_DASHBOARD_SECRET=change-me
